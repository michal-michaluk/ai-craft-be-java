apiVersion: v1
kind: Service
metadata:
  name: devices-configuration
  labels:
    app: devices-configuration
spec:
  selector:
    app: devices-configuration
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
